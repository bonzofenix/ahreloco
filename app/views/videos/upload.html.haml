%h2.title
  nuevo video
%p
  Step 2: subi el video para concursar.
= form_tag @upload_info[:url], multipart: true do
  = hidden_field_tag :token, @upload_info[:token]
  = label_tag :file
  = file_field_tag :file
  = submit_tag 'Subir video', class: 'btn'
