
= link_to "Ahreloco", root_path, class: 'brand'
%ul.nav
  %li.dropdown.programs
    %a.dropdown-toggle.programs-btn(href="#")
      Programas
      %b.caret
    %ul.dropdown-menu
      - Program.all.each do |p|
        %li.program
          %a
            = image_tag p.avatar_url
            =  p.name
        .channels.hide
          %li
            default borrame
          -p.users do |u|
            %li.channel= link_to u.username, users_path(u)
%ul.nav.pull-right
  %li
    = link_to 'Concursa con tu video', new_video_path
    - if user_signed_in?
      %li
        = link_to current_user do
          = image_tag 'https://lh6.googleusercontent.com/-z2w28o9Tl34/AAAAAAAAAAI/AAAAAAAAAAA/uzFJ5Ce58oY/s27-c/photo.jpg'
      %li.dropdown
        %a.dropdown-toggle(href="#" data-toggle="dropdown")
          = current_user.username
          %b.caret
        %ul.dropdown-menu
          %li= link_to "My Profile", current_user
          %li= link_to "Log Out", signout_path
    -  else
      %li
        = link_to "Loggeate con tu cuenta de youtube", '/auth/youtube'
